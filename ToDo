Add arg option to adjust image scaling
Add options to change stroke and highlight weight scaling values
Check if output file exists and rename (or bail?)
Check for unknown input fields
Fix-up filename extensions

